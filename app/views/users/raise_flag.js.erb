if($('#red_flag_raiser').length >0){
  $('#red_flag_raiser').html("<%= escape_javascript(render partial: 'users/red_flag') %>");
}

if($('#users_on_wall_of_shame').length >0){
  $('#users_on_wall_of_shame').html("<%= escape_javascript(render partial: 'red_flags/walled_users', locals: {users: @flagged_users}) %>");
}