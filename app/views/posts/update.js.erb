$('#remote_post_form').animate({
        height: "toggle",
        opacity: 0
      }, 500);

setTimeout(function(){
  $('#remote_post_form').html("<%=escape_javascript(render 'form') %>");
        }, 500);

$('#users_post_list').html("<%= escape_javascript(render partial: 'post', locals: {posts: @user_posts}) %>");
$('#new_post').fadeIn(500);

